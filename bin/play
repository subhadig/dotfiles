#!/usr/bin/env bash

if [ $# -ne 1 ]; then
    echo ERROR: Incorrect arguments. Pass the source location.
    exit 1
fi
case "$1" in
    *"youtube"*)
        echo Playing $1 in vlc as a youtube video
        yt-dlp --print urls --format best "$1" | vlc -
        ;;
    *)
        echo Playing $1 in vlc
        vlc "$1" 1>/dev/null 2>&1 &
        ;;
esac
